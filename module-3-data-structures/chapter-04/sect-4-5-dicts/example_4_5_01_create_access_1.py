# Creates myDictionary with key-value pairs below

myDictionary = {
   "sprockets" : 5,
   "widgets": 11,
   "cogs" : 3,
   "gizmos": 15
}
# Python aren’t guaranteed to preserve the order of dictionaries' values the way tuples, lists, and strings do
# Keys in dictionaries are immutable
print(myDictionary)

# Output is
# {'sprockets': 5, 'widgets': 11, 'cogs': 3, 'gizmos': 15}